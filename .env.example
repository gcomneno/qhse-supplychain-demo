# Optional overrides
BASE_URL=http://127.0.0.1:8000
DB_FILE=qhse_demo.sqlite3

# Postgres (docker compose)
# DATABASE_URL=postgresql+psycopg://qhse:qhse@localhost:5432/qhse

# --- Docker Compose ---
# NOTE: in docker compose, host is the service/container name (qhse_pg)
DATABASE_URL=postgresql+psycopg://qhse:qhse@qhse_pg:5432/qhse

JWT_SECRET=changeme
JWT_ALG=HS256
ACCESS_TOKEN_EXPIRE_MIN=60
ENV=docker
